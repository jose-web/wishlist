---
interface Props {
	name: string;
	price: number;
	img: string;
	url: string;
}

const { name, price, img, url } = Astro.props;
---

<div class="bg-white text-black border border-gray-200 rounded shadow 
	dark:bg-gray-800 dark:border-gray-700 dark:text-white">
    <a href={url} target="_blank">
        <img class="p-8 rounded-t-lg" src={img} alt="product image" />
    </a>
    <div class="px-5 pb-5">
        <a href={url}>
            <h5 class="text-xl font-semibold tracking-tight text-gray-900 dark:text-white" set:html={name}></h5>
        </a>

        <div class="flex items-center justify-between">
            <span class="text-3xl font-bold text-gray-900 dark:text-white">{price}€</span>

			<a href={url} target="_blank" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center">Go to Amazon</a>
        </div>
    </div>
</div>